all:
  hosts:
    plex:
      ansible_host: "{{ plex_ip }}"
    radarr:
      ansible_host: "{{ radarr_ip }}"
    sonarr:
      ansible_host: "{{ sonarr_ip }}"
    bazarr:
      ansible_host: "{{ bazarr_ip }}"
    lidarr:
      ansible_host: "{{ lidarr_ip }}"
    prowlarr:
      ansible_host: "{{ prowlarr_ip }}"
    readarr:
      ansible_host: "{{ readarr_ip }}"
    overserr:
      ansible_host: "{{ overseerr_ip }}"
    flaresolverr:
      ansible_host: "{{ flaresolverr_ip }}"
    tautulli:
      ansible_host: "{{ tautulli_ip }}"
    calibre_web:
      ansible_host: "{{ calibre_web_ip }}"
    kavita:
      ansible_host: "{{ kavita_ip }}"
    qbittorrent:
      ansible_host: "{{ qbittorrent_ip }}"
    homepage:
      ansible_host: "{{ homepage_ip }}"
    gotify:
      ansible_host: "{{ gotify_ip }}"

  vars:      
    ansible_connection: lxc
    ansible_user: root
    ansible_become: true